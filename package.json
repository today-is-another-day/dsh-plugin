{
  "name": "dsh-plugin",
  "version": "0.0.0",
  "private": true,
  "description": "DSH Web GUI plugin monorepo",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  }
}
